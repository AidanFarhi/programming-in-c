#include <stdio.h>

int main(void)
{
    // declare variables
    int val1, val2, sum;

    // assign variables and calculate their sum
    val1 = 25;
    val2 = 50;
    sum = val1 + val2;

    // display the result
    printf("The sum of %i and %i is %i\n", val1, val2, sum);
}